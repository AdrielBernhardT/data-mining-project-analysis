# Dashboard & visualisasi
dash>=4.0
plotly>=6.0

# Data & backend
duckdb>=1.0
pandas>=2.2
numpy>=1.26
pyarrow>=16.0

# Server produksi (dipakai Render lewat Start Command)
gunicorn>=22.0

scikit-learn
umap-learn

# CATATAN:
# - elasticsearch & prefect SENGAJA tidak dimasukkan di sini.
#   Keduanya cuma dipakai saat proses BUILD pipeline (pipeline/flow.py),
#   bukan saat dashboard berjalan (app.py). File .duckdb sudah dibangun
#   duluan dan tinggal dipakai backend duckdb_backend.py.
# - Kalau suatu saat mau menjalankan ulang pipeline (pipeline/flow.py) di
#   server ini juga, pasang manual: pip install elasticsearch prefect
